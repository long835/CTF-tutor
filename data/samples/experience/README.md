# Experience samples

Small **local** artifacts for tutor labs (not full CTF challenges).

| Dir | Purpose |
|-----|---------|
| `crypto_xor/` | Single-byte XOR hex ciphertext |
| `web_jwt/` | JWT header/payload example (alg confusion teaching) |
| `misc_encoding/` | Nested base64+hex string |

These are teaching fixtures. Provenance: `synthetic-lab`.
